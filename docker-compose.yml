# TEE (Tessera Embeddings Explorer) Docker Compose
#
# Usage:
#   docker-compose up -d        # Start in background
#   docker-compose logs -f      # View logs
#   docker-compose down         # Stop
#
# Data is persisted in ~/tee_data on the host

services:
  web:
    build: .
    command: ["python3", "-m", "waitress", "--host=0.0.0.0", "--port=8001", "tee_project.wsgi:application"]
    ports:
      - "8001:8001"
    volumes:
      - ~/tee_data:/data
      - ./viewports:/app/viewports
    environment:
      - TEE_DATA_DIR=/data
      - TEE_APP_DIR=/app
      - TEE_MODE=production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  tiles:
    build: .
    command: ["python3", "tile_server.py", "--prod", "--host", "0.0.0.0", "--port", "5125"]
    ports:
      - "5125:5125"
    volumes:
      - ~/tee_data:/data
    environment:
      - TEE_DATA_DIR=/data
      - TEE_APP_DIR=/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5125/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
