# TEE (Tessera Embeddings Explorer) - Dependencies
# Install: pip install -r requirements.txt

# =============================================================================
# CORE DEPENDENCIES (required for web app)
# =============================================================================

# Web server
django>=4.2
django-cors-headers>=4.0.0
waitress>=3.0.0

# Geospatial processing
geotessera>=0.7.5
# Tessera VQ bolt-on (plug-compatible drop-in for GeoTessera; per-viewport fast
# path with residual VQ). See deployment_plan.md and api/embeddings_provider.py.
# v0.3.2 trims core deps to numpy + affine; everything else (Flask, sklearn,
# pandas, …) is behind extras tessera-vq[server] / [sweep] which TEE doesn't
# need — only the client surface is used.
tessera-vq @ git+https://github.com/sk818/tessera-vq.git@v0.5.2
# tessera-eval: habitat/land-cover classifier evaluation on Tessera embeddings.
# Its own repo (extracted from this tree); used by process_viewport (zarr_utils),
# lib/evaluation_engine, scripts/tee_evaluate, and the tee-compute server.
tessera-eval @ git+https://github.com/ucam-eo/tessera-eval.git@v1.0.0
numpy>=1.24.0
rasterio>=1.3.0
Pillow>=10.0.0

# Tile server
rio-tiler>=6.0.0

# Vectorized computations
scipy>=1.10.0

# Authentication
bcrypt>=4.0.0

# HTTP client
requests>=2.28.0

# =============================================================================
# OPTIONAL: UMAP visualization (Panel 4 in advanced viewer)
# =============================================================================
umap-learn>=0.5.0

# =============================================================================
# OPTIONAL: Evaluation / learning curves (Validation panel)
# =============================================================================
scikit-learn>=1.2.0
# Pin xgboost <2.1: from 2.1 onward, the Linux wheel declares
# `Requires-Dist: nvidia-nccl-cu12; platform_system == "Linux"`, pulling a
# 300 MB CUDA NCCL wheel that TEE never uses (we only ever run xgboost on
# CPU for the validation panel). 2.0.x is the last NCCL-free major.
xgboost>=1.7.0,<2.1

# =============================================================================
# OPTIONAL: Data download sources (only needed for creating new viewports)
# =============================================================================
# Uncomment if using Google Earth Engine:
# earthengine-api

# Uncomment if using Microsoft Planetary Computer:
# pystac-client
# planetary-computer
# odc-stac
# rioxarray

# =============================================================================
# UTILITIES
# =============================================================================
tqdm>=4.65.0
pyyaml>=6.0
