"""Evaluation endpoints — removed.

All ML evaluation now runs on the compute server (tee-compute).
See packages/tessera-eval/tessera_eval/server.py.

This file is kept as a stub so existing imports don't break during
the transition. It will be deleted in a future release.
"""
